server:
  port: 80

# 热部署
debug: true

spring:
  devtools:
    restart:
      enabled: true  #设置开启热部署
  freemarker:
    cache: false    #页面不加载缓存，修改即时生效
  datasource:
    url: jdbc:mysql://49.232.1.117:3306/auth?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: hanhan
    password: HanHan_1024
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    cache: false
  mvc:
    servlet:
      path: /

  redis:
    database: 1 #redis数据库索引，默认为0
    host: localhost #服务地址
    port: 6379 #端口
    password: #密码，默认为空，本地没有设置密码，所以这里不填写
    timeout: 5000ms #连接超时时间（单位为毫秒）
    lettuce:
      pool:
        max-active: 8 #连接池最大的连接数，若使用负值表示没有限制
        max-wait: -1ms #连接池最大阻塞等待时间，若使用负值表示没有限制
        max-idle: 8 #连接池中的最大空闲连接
        min-idle: 0 #连接池中的最小空闲连接
